"""Dev-only tooling for the publishing checker (evaluation and stability harnesses).

Importable as a package (the editable install puts the repo root on ``sys.path``)
so the harness logic can be unit-tested; not shipped in the wheel.
"""
